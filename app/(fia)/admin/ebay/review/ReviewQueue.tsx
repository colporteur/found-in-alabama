// Phase eBay-1.1: this component was used by the old review-queue page,
// which has been replaced by the auto-categorize flow. Kept as an empty
// stub so the build doesn't break on its old imports. Safe to delete.

export default function ReviewQueue() {
  return null;
}
